version: 2
jobs:
  e2e:
    machine:
      enabled: true
    steps:
      - checkout
      - run: test/end_to_end/setup/setup_environment.sh
#      - run:
#          name: Deploy executors to clusters
#      - run:
#          name: Run API
#      - run:
#          name: Run e2e tests

workflows:
  version: 2
  build-and-test:
    jobs:
      - e2e