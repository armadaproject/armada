issues:
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  sort-results: true

linters:
  disable-all: true
  enable:
    - lll
    - gofumpt
    - deadcode
    - goimports
    - ineffassign
    - misspell

linters-settings:
  lll:
    line-length: 261
  misspell:
    locale: UK
    ignore-words:
      - analyze
      - serialize

