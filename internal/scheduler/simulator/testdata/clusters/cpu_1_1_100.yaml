name: "1 CPU cluster with 100 nodes"
pools:
  - name: "CPU"
    clusterGroups:
      - clusters:
          - name: "cpu-01"
            nodeTemplates:
              - number: 100
                totalResources:
                  resources:
                    cpu: "32"
                    memory: "1024Gi"
