name: "3 CPU clusters with 100 nodes each in a single group"
pools:
  - name: "CPU"
    clusterGroups:
      - clusters:
          - name: "cpu-01"
            nodeTemplates:
              - number: 100
                totalResources:
                  resources:
                    cpu: "32"
                    memory: "1024Gi"
          - name: "cpu-02"
            nodeTemplates:
              - number: 100
                totalResources:
                  resources:
                    cpu: "32"
                    memory: "1024Gi"
          - name: "cpu-03"
            nodeTemplates:
              - number: 100
                totalResources:
                  resources:
                    cpu: "32"
                    memory: "1024Gi"
