// Package queryapi implements the Armada server's Jobs gRPC service (the
// "Query API"), which serves programmatic job-status queries by reading the
// Lookout database (the job, job_run, job_spec and job_error tables) directly
// via the server's own connection pool.
//
// The QueryApi type in query_api.go implements each RPC by issuing type-safe
// queries generated by sqlc (see the database subpackage).
//
// mirror.go provides optional, opt-in server-side query mirroring: when
// enabled, a QueryDB wrapper replays each query against a second database in a
// bounded, fire-and-forget manner so that database can be evaluated under real
// Query API load. Mirror results and errors are discarded and never affect the
// primary query path.
package queryapi
