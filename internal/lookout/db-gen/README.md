A tool to quickly populate Lookout database

```
Usage: db-gen [OPTIONS]

OPTIONS:  

    --host <host> (default: "localhost")
    
    --user <user> (default: "postgres")

    --password <password> (default: "psw")

    --port <port> (default: 5432)
```