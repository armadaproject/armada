# Broadside Configuration Files

This directory contains user-defined YAML configuration files for Broadside load
tests.

Files in this directory are gitignored and will not be committed to the
repository.

For example configurations, see the `./examples/` directory.
