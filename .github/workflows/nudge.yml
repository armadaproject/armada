name: Nudge

on:
  workflow_run:
    workflows: [Go, Go End to End]
    types: [completed]
    branches: [master]


jobs:
  nudge:
    runs-on: ubuntu-latest
    environment: nudge
    steps:
      - name: Send notification
        uses: pavlovic-ivan/octo-nudge@v1
        with:
          webhooks: ${{ secrets.NUDGE_WEBHOOKS }}