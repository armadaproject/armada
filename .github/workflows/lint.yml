name: "Lint"

on:
  push:
    branches-ignore:
      - master
      - main
  pull_request:
    branches:
      - master
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
# TODO: will enable linters in subsequent PR so changes are small
#  lint-proto:
#    name: Lint proto
#    runs-on: ubuntu-22.04
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#
#      - name: Lint using protolint
#        uses: plexsystems/protolint-action@v0.7.0
#        with:
#          configDirectory: .
#          srcDirectory: pkg

#  TODO: will enable linters in subsequent PR so changes are small
#  lint-docs:
#    name: Lint docs
#    runs-on: ubuntu-22.04
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#
#      - name: Lint changelog file
#        uses: avto-dev/markdown-lint@v1
#        with:
#          args: './docs'

  lint-go:
    name: Lint Go
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.20

      - name: Lint using golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: v1.52.2
          only-new-issues: true
          args: --timeout=10m --issues-exit-code=1 --sort-results ./...

#  TODO: will enable linters in subsequent PR so changes are small
#  lint-helm:
#    name: Lint Helm charts
#    runs-on: ubuntu-22.04
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#
#      - name: Lint Helm charts
#        uses: ./.github/workflows/linters/lint-helm

#  TODO: will enable linters in subsequent PR so changes are small
#  lint-docker:
#    name: Lint Dockerfiles
#    runs-on: ubuntu-22.04
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#
#      - name: Lint Dockerfiles
#        uses: ./.github/workflows/linters/lint-docker
